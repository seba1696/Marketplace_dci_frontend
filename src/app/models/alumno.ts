export class Alumno {
    constructor(public id: number, public nombre: string, public matricula: string,
        public credito: number, public codigoCarrera: string, public email: string,
        public idUsuario: number) { }
}